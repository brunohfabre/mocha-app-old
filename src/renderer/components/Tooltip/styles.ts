import * as RadixTooltip from '@radix-ui/react-tooltip'
import { styled } from '@styles/index'

export const Container = styled(RadixTooltip.Content, {
  backgroundColor: '#000000',
  color: '#ffffff',
  padding: 4,
  borderRadius: 4,
  fontSize: '14px',
})
